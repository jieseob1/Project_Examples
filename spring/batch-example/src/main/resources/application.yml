spring:
  jpa:
    database: h2
    hibernate:
      ddl-auto: create
    show-sql: true
    datasource:
      url: jdbc:h2:mem:testdb
      username: sa
      driver-class-name: org.h2.Driver
    h2:
        console:
            enabled: true
            path: /h2-console
